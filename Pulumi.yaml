name: mursion-deployment
runtime:
  name: nodejs
  options:
    packagemanager: npm
description: A TypeScript program to deploy a containerized service on AWS
config:
  pulumi:tags:
    value:
      pulumi:template: container-aws-typescript